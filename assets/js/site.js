(()=>{"use strict";const menu=document.querySelector("[data-menu]");const nav=document.querySelector("#site-nav");const toast=document.querySelector("#toast");function close(){if(!menu||!nav)return;nav.classList.remove("open");menu.setAttribute("aria-expanded","false")}if(menu&&nav){menu.addEventListener("click",()=>{const open=nav.classList.toggle("open");menu.setAttribute("aria-expanded",String(open))});nav.addEventListener("click",e=>{if(e.target.closest("a"))close()});document.addEventListener("keydown",e=>{if(e.key==="Escape"){close();menu.focus()}});document.addEventListener("click",e=>{if(!nav.contains(e.target)&&!menu.contains(e.target))close()})}document.querySelectorAll("[data-copy]").forEach(button=>button.addEventListener("click",async()=>{const value=button.dataset.copy;try{await navigator.clipboard.writeText(value);show("已复制："+value)}catch{window.prompt("请手动复制：",value)}}));function show(message){if(!toast)return;toast.textContent=message;toast.classList.add("show");clearTimeout(show.timer);show.timer=setTimeout(()=>toast.classList.remove("show"),2400)}document.querySelectorAll('a[target="_blank"]').forEach(a=>{a.rel="noopener noreferrer";a.classList.add("external")})})();
